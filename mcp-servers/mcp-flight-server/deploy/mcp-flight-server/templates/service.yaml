apiVersion: v1
kind: Service
metadata:
  name: {{ include "flight-server.fullname" . }}
  labels:
    {{- include "flight-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "flight-server.selectorLabels" . | nindent 4 }}
